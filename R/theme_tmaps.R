theme_tmaps<-tm_layout(
panel.label.bg.color="white", # Change panel label to white
frame=FALSE, # No frame
frame.lwd = NA, # No frame
panel.label.fontface="bold", # Bold panel label
attr.outside=TRUE) # Attributes outside